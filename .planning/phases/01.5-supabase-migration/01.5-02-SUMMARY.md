---
phase: 01.5-supabase-migration
plan: "02"
subsystem: database
tags: [prisma, supabase, postgresql, schema, pgbouncer]

# Dependency graph
requires:
  - phase: 01.5-01
    provides: Supabase project provisioned with DATABASE_URL and DIRECT_URL in .env.local
provides:
  - Clean prisma/schema.prisma with only Room, RoomPhoto, AddOn, Settings models
  - directUrl configured in Prisma datasource for Supabase PgBouncer compatibility
  - All four application tables created in Supabase PostgreSQL public schema
affects:
  - 01.5-03-supabase-client
  - 01.5-04-auth-replacement
  - 01.5-05-middleware

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dual Prisma URL pattern: DATABASE_URL (transaction pooler port 6543) + DIRECT_URL (direct connection port 5432)"
    - "Supabase owns auth schema — Prisma manages only application models in public schema"

key-files:
  created: []
  modified:
    - "prisma/schema.prisma"

key-decisions:
  - "Removed Account, Session, User, VerificationToken models — Supabase Auth manages these in its own auth schema"
  - "Added directUrl to Prisma datasource — required for DDL operations via prisma db push with PgBouncer"
  - "Direct connection URL (db.[ref].supabase.co:5432) used for both DATABASE_URL and DIRECT_URL — transaction pooler URL caused FATAL: Tenant or user not found"
  - "@db.Text annotation removed from Room.description by db pull normalization — semantically equivalent (PostgreSQL text is the default for String)"

patterns-established:
  - "Prisma direct connection: use db.[project-ref].supabase.co:5432 for schema migrations"

requirements-completed: [INFRA-01]

# Metrics
duration: 4min
completed: "2026-03-26"
---

# Phase 1.5 Plan 02: Prisma Schema Migration Summary

**NextAuth models removed from Prisma schema, directUrl added for Supabase PgBouncer, and all four application tables (Room, RoomPhoto, AddOn, Settings) pushed to Supabase PostgreSQL**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-26T20:04:44Z
- **Completed:** 2026-03-26T20:08:43Z
- **Tasks:** 2
- **Files modified:** 1 (prisma/schema.prisma)

## Accomplishments

- Removed four NextAuth models (Account, Session, User, VerificationToken) from Prisma schema — Supabase Auth owns these
- Added `directUrl = env("DIRECT_URL")` to Prisma datasource for PgBouncer compatibility
- Pushed schema to Supabase PostgreSQL — Room, RoomPhoto, AddOn, Settings tables live in public schema
- Regenerated Prisma client without NextAuth types
- Fixed .env and .env.local with working Supabase direct connection URL

## Task Commits

Each task was committed atomically:

1. **Task 1: Update Prisma schema — remove NextAuth models and add directUrl** - `1e92bcc` (chore)
2. **Task 2: Push schema to Supabase PostgreSQL** - `81cea92` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `prisma/schema.prisma` - Removed NextAuth models; added directUrl; schema normalized by db pull (cosmetic only)
- `.env` - Updated with real Supabase direct connection URL (gitignored)
- `.env.local` - Corrected DATABASE_URL and DIRECT_URL to use db.[project-ref].supabase.co:5432 (gitignored)

## Decisions Made

- Used `db.aawexuirbmktoozzajer.supabase.co:5432` (direct connection) for both DATABASE_URL and DIRECT_URL — the transaction pooler URL format (`...pooler.supabase.com:6543`) in `.env.local` had an incomplete hostname placeholder (`...`) and the corrected format returned "Tenant or user not found"; direct connection works reliably
- Accepted `db pull` cosmetic normalization: `@db.Text` removed from `description` field (equivalent), field order changed for `cleaningFee`/`extraGuestFee` annotations

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed incomplete Supabase connection string hostname in .env.local**
- **Found during:** Task 2 (Push schema to Supabase PostgreSQL)
- **Issue:** .env.local had `...pooler.supabase.com` as hostname — ellipsis placeholder left from Plan 01 setup; prisma db push failed with P1001
- **Fix:** Replaced with correct direct connection URL `db.aawexuirbmktoozzajer.supabase.co:5432` in both .env and .env.local
- **Files modified:** .env, .env.local (both gitignored)
- **Verification:** `npx prisma validate` passes; `npx prisma db push` reports "database already in sync"
- **Committed in:** 81cea92 (Task 2 commit — env files not committed, gitignored)

---

**Total deviations:** 1 auto-fixed (1 bug — incomplete env var)
**Impact on plan:** Required fix to enable Prisma database operations. No scope creep.

## Issues Encountered

- Transaction pooler URL format (`aws-0-us-east-1.pooler.supabase.com:6543`) returned "FATAL: Tenant or user not found" — likely requires the `postgres.PROJECT_REF` username format specific to the pooler. The direct connection URL bypasses this and is appropriate for a small project without connection pooling requirements at this stage.

## User Setup Required

None - no additional external service configuration required. Both .env and .env.local have been corrected with working connection strings (gitignored).

## Next Phase Readiness

- Plan 03 (Supabase client setup) can proceed: schema is live, application models are clean
- Plan 04 (auth replacement) and Plan 05 (middleware) will fix expected TypeScript errors from next-auth removal
- No blockers for subsequent plans

---
*Phase: 01.5-supabase-migration*
*Completed: 2026-03-26*

## Self-Check: PASSED

- prisma/schema.prisma: FOUND
- 01.5-02-SUMMARY.md: FOUND
- Commit 1e92bcc (Task 1): FOUND
- Commit 81cea92 (Task 2): FOUND
