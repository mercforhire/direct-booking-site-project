---
phase: 01.5-supabase-migration
plan: "01"
subsystem: infra
tags: [supabase, nextjs, auth, postgresql, prisma]

# Dependency graph
requires: []
provides:
  - Supabase project created with landlord user seeded and Resend SMTP configured
  - "@supabase/supabase-js and @supabase/ssr installed; next-auth and @auth/prisma-adapter removed"
  - ".env.local has all five Supabase credentials (URL, anon key, service role, DATABASE_URL, DIRECT_URL)"
affects:
  - 01.5-02-schema-migration
  - 01.5-03-supabase-client
  - 01.5-04-auth-replacement
  - 01.5-05-middleware

# Tech tracking
tech-stack:
  added: ["@supabase/supabase-js ^2.100.1", "@supabase/ssr ^0.9.0"]
  patterns: ["Supabase client pattern (to be established in plan 03)", "Magic link auth via Supabase Auth"]

key-files:
  created: []
  modified: ["package.json", "package-lock.json", ".env.local (user-configured)"]

key-decisions:
  - "Supabase chosen as managed PostgreSQL + Auth provider to replace Prisma-backed NextAuth"
  - "Resend configured as Supabase SMTP provider for magic link emails (reuses existing RESEND_API_KEY)"
  - "next-auth and @auth/prisma-adapter removed; TypeScript errors in auth.ts/auth-edge.ts/middleware.ts are expected and will be resolved in plans 03-04"
  - "DATABASE_URL uses transaction pooler (port 6543 + ?pgbouncer=true); DIRECT_URL uses session pooler (port 5432) for Prisma migrations"

patterns-established:
  - "Supabase credentials pattern: NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY (client-safe) + SUPABASE_SERVICE_ROLE_KEY (server-only)"

requirements-completed: [INFRA-01]

# Metrics
duration: 5min
completed: "2026-03-26"
---

# Phase 1.5 Plan 01: Supabase Project Setup & Package Swap Summary

**Supabase project provisioned with landlord user, Resend SMTP, and magic link redirect URLs; @supabase/supabase-js and @supabase/ssr installed, next-auth and @auth/prisma-adapter removed**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-26T20:02:01Z
- **Completed:** 2026-03-26T20:07:00Z
- **Tasks:** 2 (1 human-action, 1 auto)
- **Files modified:** 2 (package.json, package-lock.json)

## Accomplishments

- Supabase project created with landlord admin user seeded in Supabase Auth and Resend SMTP configured
- Redirect URLs added for magic link auth (localhost and production)
- .env.local updated with all five required Supabase credentials; legacy NextAuth vars removed
- @supabase/supabase-js ^2.100.1 and @supabase/ssr ^0.9.0 installed; next-auth and @auth/prisma-adapter uninstalled

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Supabase project and configure credentials** - Human-action checkpoint (no commit — user-performed)
2. **Task 2: Swap npm packages** - `3331717` (chore)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `package.json` - Added @supabase/supabase-js, @supabase/ssr; removed next-auth, @auth/prisma-adapter
- `package-lock.json` - Updated lockfile reflecting package changes
- `.env.local` - User-configured with Supabase credentials (not committed — gitignored)

## Decisions Made

- Used transaction pooler (port 6543 + ?pgbouncer=true) for DATABASE_URL to support Prisma with connection pooling; session pooler (port 5432) for DIRECT_URL for direct migrations
- Kept RESEND_API_KEY (reused as Supabase SMTP password) and all UPLOADTHING_* vars; removed NEXTAUTH_SECRET, NEXTAUTH_URL, AUTH_RESEND_KEY, AUTH_FROM_EMAIL
- TypeScript errors in auth.ts, auth-edge.ts, and middleware.ts left intentionally — these files are replaced in plans 03 and 04

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

Task 1 was a human-action checkpoint. The user completed all Supabase dashboard configuration:
- Created Supabase project (region: us-east-1)
- Created landlord admin user in Supabase Auth > Users
- Configured Resend as custom SMTP (smtp.resend.com, port 465)
- Added redirect URLs: http://localhost:3000/**, production URL/**
- Populated .env.local with NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, DATABASE_URL, DIRECT_URL

## Next Phase Readiness

- Plan 02 (schema migration) can proceed: DATABASE_URL and DIRECT_URL are configured for Prisma
- Plan 03 (Supabase client setup) can proceed: @supabase/supabase-js and @supabase/ssr are installed
- Plans 04 (auth replacement) and 05 (middleware) will fix the expected TypeScript errors from next-auth removal
- No blockers for subsequent plans

---
*Phase: 01.5-supabase-migration*
*Completed: 2026-03-26*
