---
phase: 03-guest-room-browsing
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: false
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-04

must_haves:
  truths:
    - "Guest can visually verify photo gallery, lightbox, and horizontal scroll strip"
    - "Guest can verify filter greys out rooms correctly and URL updates on filter change"
    - "Guest can verify nightly estimate calculates correctly when navigating from list to detail"
    - "Guest can verify fee structure shows all rows with correct zero-value labels"
  artifacts:
    - path: "src/app/rooms/page.tsx"
      provides: "Public /rooms list page — verified accessible without auth"
    - path: "src/app/rooms/[id]/page.tsx"
      provides: "Full detail page — verified all sections visible"
  key_links: []
---

<objective>
Human visual verification of the complete guest room browsing experience.

Purpose: Confirm all visual, interactive, and navigation behaviors work end-to-end — behaviors that cannot be verified by automated tests (photo gallery UI, filter greying, URL carry-over, nightly estimate).
Output: Sign-off that Phase 3 guest room browsing is complete and ready for Phase 4.
</objective>

<execution_context>
@/Users/LeonChen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/LeonChen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-guest-room-browsing/03-CONTEXT.md
@.planning/phases/03-guest-room-browsing/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite run</name>
  <files></files>
  <action>
    Run the full test suite to confirm all tests pass before human verification.

    npm test

    If any tests fail, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All tests green. Zero failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — Phase 3 complete</name>
  <action>Human verifies all visual, interactive, and navigation behaviors in the browser. See how-to-verify for exact steps.</action>
  <verify>
    <automated>MISSING — this task requires human verification in the browser</automated>
  </verify>
  <done>All 7 scenarios pass and user types "approved".</done>
  <what-built>
    Complete Phase 3 guest room browsing implementation:
    - /rooms list page with full-width tiles, cover photos, date range filter, guest count filter, and greyed-out unavailable rooms
    - /rooms/[id] detail page with hero photo gallery (lightbox), pricing table, add-ons, availability calendar, and disabled Request to Book button
    - Middleware fix: /rooms is now a public route
    - Availability filter logic: blocked dates, booking window, guest count checks
  </what-built>
  <how-to-verify>
    Start the dev server: npm run dev

    **Scenario 1 — Room list page accessible without login:**
    1. Open a private/incognito browser window (no admin session)
    2. Navigate to http://localhost:3000/rooms
    3. Expected: Room list loads — NOT redirected to /login

    **Scenario 2 — Room list tiles:**
    1. Verify each room tile shows: cover photo (or grey placeholder if no photos), room name, base nightly rate, max guest count
    2. Verify tile layout is full-width horizontal (photo left, info right)
    3. Verify tiles have hover effects

    **Scenario 3 — Filter + greyed-out rooms:**
    1. In the filter header, pick check-in/check-out dates that overlap blocked dates on one room (use the admin availability calendar to find or create a blocking)
    2. Expected: That room's tile becomes greyed out with "Unavailable for these dates" badge
    3. Expected: URL updates to ?checkin=...&checkout=...&guests=N
    4. Expected: Greyed-out tile is still clickable (navigates to detail page)

    **Scenario 4 — URL params carry forward:**
    1. Navigate to http://localhost:3000/rooms?checkin=2026-05-01&checkout=2026-05-04&guests=2
    2. Click any room tile
    3. Expected: Detail page URL includes the same checkin, checkout, guests params
    4. Expected: Pricing section shows "3 nights × $X.XX = $Y.YY"

    **Scenario 5 — Room detail page full content:**
    1. Navigate to any room detail page /rooms/[id]
    2. Verify: full-width hero photo at top with correct aspect ratio
    3. Verify: horizontal photo scroll strip visible if room has >1 photo
    4. Verify: clicking any photo (hero or thumbnail) opens lightbox
    5. Verify: lightbox prev/next arrows cycle through photos; Escape key closes
    6. Verify: room name, location label, plain-text description with visible line breaks
    7. Verify: Pricing section shows base nightly rate, cleaning fee, extra guest fee, add-ons
    8. Verify: cleaning fee shows "Included" if $0; extra guest fee shows "No extra guest fee" if $0
    9. Verify: add-on with price $0 shows "Free"
    10. Verify: "Final price set by landlord at approval" footnote is present
    11. Verify: Availability calendar renders below pricing section
    12. Verify: "Request to Book" button is visible but disabled/greyed — cannot be clicked

    **Scenario 6 — No photos fallback:**
    1. If a room exists with no photos: verify grey placeholder div with icon appears (not a broken image)

    **Scenario 7 — Admin routes still protected:**
    1. In the incognito window, navigate to http://localhost:3000/rooms/new
    2. Expected: redirected to /login (admin route still protected)
  </how-to-verify>
  <resume-signal>Type "approved" if all scenarios pass, or describe which scenarios failed with details.</resume-signal>
</task>

</tasks>

<verification>
npm test green (Task 1).
All 7 manual verification scenarios pass (Task 2 checkpoint).
</verification>

<success_criteria>
1. npm test passes with zero failures
2. /rooms accessible without auth — no redirect
3. Room tiles render correctly with photos and pricing info
4. Filter greyes out unavailable rooms and updates URL params
5. URL params carry from list to detail, nightly estimate calculates
6. Detail page shows all sections: gallery, description, pricing, calendar, disabled CTA
7. Lightbox opens and cycles through photos
8. Admin routes (/rooms/new, edit) still protected
</success_criteria>

<output>
After completion, create .planning/phases/03-guest-room-browsing/03-03-SUMMARY.md
</output>
