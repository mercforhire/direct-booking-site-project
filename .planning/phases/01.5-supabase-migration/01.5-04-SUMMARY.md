---
phase: 01.5-supabase-migration
plan: "04"
subsystem: auth
tags: [supabase, next-auth, migration, server-actions, otp, magic-link]

requires:
  - phase: 01.5-supabase-migration/01.5-03
    provides: Supabase server/client helper modules (createClient for server and browser)

provides:
  - All server actions (room.ts, settings.ts, room-photos.ts) use Supabase getUser() auth guard
  - Login page uses Supabase signInWithOtp with shouldCreateUser: false
  - UploadThing middleware uses Supabase getUser() auth guard
  - Sidebar uses Supabase signOut
  - NextAuth infrastructure (auth.ts, auth-edge.ts, [...nextauth] route) fully deleted
  - Zero next-auth references anywhere in src/

affects:
  - 01.5-05
  - Any future server actions or protected routes

tech-stack:
  added: []
  patterns:
    - "Supabase server-side auth guard: const supabase = await createClient(); const { data: { user }, error } = await supabase.auth.getUser(); if (error || !user) throw new Error('Unauthorized')"
    - "Supabase browser signOut: const supabase = createClient(); await supabase.auth.signOut(); router.push('/login')"
    - "Supabase OTP login: supabase.auth.signInWithOtp({ email, options: { shouldCreateUser: false, emailRedirectTo: origin + '/auth/confirm' } })"

key-files:
  created: []
  modified:
    - src/actions/room.ts
    - src/actions/settings.ts
    - src/actions/room-photos.ts
    - src/app/(auth)/login/page.tsx
    - src/app/api/uploadthing/core.ts
    - src/components/admin/sidebar.tsx
    - tests/actions/room.test.ts
    - tests/actions/settings.test.ts
  deleted:
    - src/lib/auth.ts
    - src/lib/auth-edge.ts
    - src/app/api/auth/[...nextauth]/route.ts

key-decisions:
  - "shouldCreateUser: false in signInWithOtp prevents auto-registration of unknown emails — admin-only access enforced at OTP level"
  - "Supabase signOut used in sidebar instead of next-auth signOut — redirects to /login via router.push"
  - "Test mocks updated to mock @/lib/supabase/server createClient returning { auth: { getUser } } instead of @/lib/auth"

patterns-established:
  - "Supabase requireAuth() helper pattern: createClient + getUser() for all server actions"
  - "signInWithOtp with shouldCreateUser: false for admin-only magic link login"

requirements-completed:
  - INFRA-01

duration: 2min
completed: "2026-03-26"
---

# Phase 01.5 Plan 04: NextAuth Removal & Supabase Auth Migration Summary

**All three server actions, UploadThing middleware, and sidebar replaced NextAuth auth() with Supabase getUser(); login page migrated to signInWithOtp OTP; NextAuth infrastructure deleted; zero next-auth references remain in src/**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-26T20:13:55Z
- **Completed:** 2026-03-26T20:16:00Z
- **Tasks:** 2
- **Files modified:** 8 (plus 3 deleted)

## Accomplishments

- Replaced NextAuth `auth()` auth guards in room.ts, settings.ts, room-photos.ts with Supabase `createClient() + getUser()` pattern
- Rewrote login page to use `supabase.auth.signInWithOtp` with `shouldCreateUser: false` for admin-only magic link flow
- Deleted NextAuth infrastructure: auth.ts, auth-edge.ts, src/app/api/auth/[...nextauth]/route.ts
- Auto-fixed two additional files with stale next-auth imports (uploadthing/core.ts, sidebar.tsx)
- Updated Vitest test mocks from `@/lib/auth` to `@/lib/supabase/server` mock pattern
- TypeScript build passes clean with zero errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Update server actions to use Supabase auth guard** - `cfc2093` (feat)
2. **Task 2: Rewrite login page and delete NextAuth infrastructure** - `aa86fe6` (feat)

## Files Created/Modified

- `src/actions/room.ts` - requireAuth() now uses createClient + getUser()
- `src/actions/settings.ts` - inline auth guard uses createClient + getUser()
- `src/actions/room-photos.ts` - requireAuth() now uses createClient + getUser()
- `src/app/(auth)/login/page.tsx` - handleSubmit uses signInWithOtp (shouldCreateUser: false)
- `src/app/api/uploadthing/core.ts` - middleware uses createClient + getUser() (auto-fixed)
- `src/components/admin/sidebar.tsx` - sign-out uses supabase.auth.signOut + router.push (auto-fixed)
- `tests/actions/room.test.ts` - mocks updated to Supabase server client pattern
- `tests/actions/settings.test.ts` - mocks updated to Supabase server client pattern
- **Deleted:** src/lib/auth.ts, src/lib/auth-edge.ts, src/app/api/auth/[...nextauth]/route.ts

## Decisions Made

- `shouldCreateUser: false` in `signInWithOtp` enforces admin-only access at the OTP level — unknown emails receive no magic link
- Sidebar signOut redirects to `/login` via `router.push` after `supabase.auth.signOut()`
- Test mocks restructured to return `{ data: { user }, error }` matching Supabase getUser() response shape

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed stale next-auth import in UploadThing core.ts**
- **Found during:** Task 2 (delete NextAuth infrastructure)
- **Issue:** `src/app/api/uploadthing/core.ts` still imported `auth` from `@/lib/auth` — would break after auth.ts deleted
- **Fix:** Replaced with `createClient` from `@/lib/supabase/server` + `getUser()` auth guard
- **Files modified:** src/app/api/uploadthing/core.ts
- **Verification:** grep for next-auth in src/ returns nothing
- **Committed in:** aa86fe6 (Task 2 commit)

**2. [Rule 3 - Blocking] Fixed stale next-auth import in sidebar.tsx**
- **Found during:** Task 2 (delete NextAuth infrastructure)
- **Issue:** `src/components/admin/sidebar.tsx` imported `signOut` from `next-auth/react` — would break at runtime after package removed
- **Fix:** Replaced with `createClient` from `@/lib/supabase/client`, added `useRouter`, implemented `handleSignOut` using `supabase.auth.signOut()`
- **Files modified:** src/components/admin/sidebar.tsx
- **Verification:** grep for next-auth in src/ returns nothing; TypeScript build passes
- **Committed in:** aa86fe6 (Task 2 commit)

**3. [Rule 3 - Blocking] Updated Vitest test mocks to use Supabase server client**
- **Found during:** Task 2 (TypeScript build check)
- **Issue:** tests/actions/room.test.ts and tests/actions/settings.test.ts still mocked and imported `@/lib/auth` (now deleted) — TypeScript error TS2307
- **Fix:** Replaced `vi.mock("@/lib/auth", ...)` with `vi.mock("@/lib/supabase/server", ...)` returning mock with `auth.getUser`; updated all test cases to use new mock interface
- **Files modified:** tests/actions/room.test.ts, tests/actions/settings.test.ts
- **Verification:** `npx tsc --noEmit --skipLibCheck` exits 0 with no errors
- **Committed in:** aa86fe6 (Task 2 commit)

---

**Total deviations:** 3 auto-fixed (all Rule 3 - Blocking)
**Impact on plan:** All auto-fixes required for build correctness after auth.ts deletion. No scope creep.

## Issues Encountered

None beyond the auto-fixed blocking issues above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Authentication stack is fully migrated to Supabase — zero next-auth references remain in src/
- All server actions, UploadThing route, and UI components use Supabase auth
- Ready for Plan 05: any remaining cleanup or end-to-end wiring tasks

## Self-Check: PASSED

All key files present, deleted files confirmed absent, both task commits verified in git log.

---
*Phase: 01.5-supabase-migration*
*Completed: 2026-03-26*
