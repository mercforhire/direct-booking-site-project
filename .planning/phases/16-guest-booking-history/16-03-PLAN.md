---
phase: 16-guest-booking-history
plan: "03"
type: execute
wave: 3
depends_on: ["16-02"]
files_modified: []
autonomous: false
requirements: [HIST-01, HIST-02, HIST-03, HIST-04]
must_haves:
  truths:
    - "Full test suite passes with no regressions"
    - "Home page returning-guest section is visually distinct and links to /guest/login?next=/my-bookings"
    - "/my-bookings page renders booking cards with correct layout, status badges, and prices"
    - "Unauthenticated /my-bookings visitors are redirected to login"
  artifacts: []
  key_links: []
---

<objective>
Run the full test suite to confirm no regressions, then human-verify the guest booking history UI end-to-end.

Purpose: Catch visual/layout issues and confirm the full flow works before marking Phase 16 complete.
Output: Verified phase with no regressions.
</objective>

<execution_context>
@/Users/LeonChen/Documents/direct booking site project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/LeonChen/Documents/direct booking site project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-guest-booking-history/16-CONTEXT.md
@.planning/phases/16-guest-booking-history/16-01-SUMMARY.md
@.planning/phases/16-guest-booking-history/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and TypeScript check</name>
  <files></files>
  <read_first>
    - package.json (to confirm test script command)
  </read_first>
  <action>
    Run the following checks in sequence:
    1. `npx tsc --noEmit` — full TypeScript compilation must pass with 0 errors
    2. `npx vitest run` — full test suite must pass with 0 failures

    If any test fails, investigate and fix. Common issues:
    - Import path changes from new files
    - Mock conflicts from new Prisma includes
    - Type mismatches from Decimal coercion changes

    Do NOT introduce new test files in this task — this is a regression check only.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - `npx vitest run` exits with code 0 and reports 0 failures
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript and all existing tests pass with zero errors and zero failures</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of guest booking history flow</name>
  <files></files>
  <action>
    Present the 6 verification scenarios to the user and wait for approval. Dev server should already be running from Task 1 verification. If not, start it with `npm run dev`.
  </action>
  <what-built>
    Home page returning-guest section, /my-bookings auth-gated page with Upcoming/Past booking sections, booking cards with room photos, status badges, and prices.
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **Scenario 1 — Home page guest entry point:**
    1. Visit http://localhost:3000
    2. Verify "Already booked with us?" text is visible below the "View Rooms" button
    3. Verify a "Sign in" button is present and visually distinct (card/border style)
    4. Verify "Admin Login" link is still at the bottom, small and muted
    5. Click "Sign in" — should navigate to /guest/login?next=/my-bookings

    **Scenario 2 — Unauthenticated /my-bookings redirect:**
    1. Open a new incognito window
    2. Visit http://localhost:3000/my-bookings
    3. Verify redirect to /guest/login?next=/my-bookings

    **Scenario 3 — Authenticated guest with bookings:**
    1. Log in as a guest who has existing bookings
    2. After login, should arrive at /my-bookings
    3. Verify personalised heading shows "[First name]'s Bookings"
    4. Verify "Sign out" link is visible (top-right area)
    5. Verify Upcoming section shows bookings with checkin >= today (soonest first)
    6. Verify Past section shows bookings with checkin < today (most recent first)
    7. Each card should show: room photo thumbnail (or gray placeholder), room name, date range, guest count, colour-coded status badge, price
    8. Status badge colours: Pending=orange, Approved=blue, Paid=green, Cancelled=gray, Declined=red
    9. Click a booking card — should navigate to /bookings/[id] (existing booking detail page)

    **Scenario 4 — Sign out:**
    1. On /my-bookings, click "Sign out"
    2. Verify redirect to home page (/)

    **Scenario 5 — Legacy /my-booking redirect:**
    1. While logged in, visit http://localhost:3000/my-booking
    2. Verify redirect to /my-bookings (the new list page)

    **Scenario 6 — Empty state (if testable):**
    1. If a guest account exists with no bookings, log in as that guest
    2. Verify "You don't have any bookings yet." message appears with a "Browse rooms" link
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification — human approval required"</automated>
  </verify>
  <done>Human approves all 6 scenarios or issues are identified and fixed</done>
  <resume-signal>Type "approved" to complete Phase 16, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- npx tsc --noEmit exits 0
- npx vitest run exits 0 with no failures
- Human confirms all 6 scenarios pass visual inspection
</verification>

<success_criteria>
Full test suite passes with no regressions. Human confirms the guest booking history UI renders correctly across all scenarios: home page entry point, auth redirect, booking cards, status badges, sign-out, and legacy redirect.
</success_criteria>

<output>
After completion, create `.planning/phases/16-guest-booking-history/16-03-SUMMARY.md`
</output>
