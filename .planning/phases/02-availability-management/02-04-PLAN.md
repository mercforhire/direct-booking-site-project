---
phase: 02-availability-management
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified: []
autonomous: false
requirements:
  - AVAIL-01
  - AVAIL-02
  - AVAIL-03
  - AVAIL-04
  - ADMIN-04

must_haves:
  truths:
    - "All Phase 2 automated tests pass"
    - "Landlord can block and unblock individual dates from the admin calendar"
    - "Landlord can block and unblock a date range from the admin calendar"
    - "Landlord can change min/max stay and booking window and see them saved"
    - "Guest calendar correctly greys out past, beyond-window, and blocked dates"
    - "Room selector switches the calendar to the selected room's data"
  artifacts:
    - path: "tests/actions/availability.test.ts"
      provides: "All server action tests passing"
    - path: "tests/validations/availability.test.ts"
      provides: "All validation schema tests passing"
  key_links:
    - from: "Admin calendar blocked date toggle"
      to: "Database BlockedDate row"
      via: "toggleBlockedDate server action"
      pattern: "toggleBlockedDate"
    - from: "Guest calendar disabled dates"
      to: "Fetched blockedDateStrings from DB"
      via: "Page server component passes to AvailabilityCalendarReadonly"
      pattern: "blockedDateStrings"
---

<objective>
End-to-end human verification of all Phase 2 features: automated test suite, admin availability dashboard (date blocking, range operations, settings panel), and guest-facing read-only calendar.

Purpose: Gate before Phase 3 — confirms all 5 requirements (AVAIL-01 through AVAIL-04 + ADMIN-04) are working in the browser.
Output: Human-verified sign-off. All Phase 2 requirements confirmed.
</objective>

<execution_context>
@/Users/LeonChen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/LeonChen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-availability-management/02-CONTEXT.md
@.planning/phases/02-availability-management/02-02-SUMMARY.md
@.planning/phases/02-availability-management/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite and build check</name>
  <files></files>
  <action>
    Run the full automated verification suite:

    1. Full test suite:
       ```bash
       npm test
       ```
       All tests must pass (including availability.test.ts and validations/availability.test.ts).

    2. Build check:
       ```bash
       npm run build
       ```
       Build must exit 0 with no TypeScript errors.

    3. Start dev server for manual verification:
       ```bash
       npm run dev
       ```

    If any automated check fails: fix the issue before proceeding to the checkpoint. Do not proceed to human verification with a failing test suite.
  </action>
  <verify>
    <automated>npm test && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm test exits 0 (all tests green), npm run build exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end human verification of Phase 2</name>
  <files></files>
  <action>
    Human verifies all 7 scenarios below with the dev server running at http://localhost:3000.
  </action>
  <verify>
    <automated>MISSING — human verification required for visual/interactive calendar behavior</automated>
  </verify>
  <done>Human types "approved" after all 7 scenarios pass</done>
  <what-built>
    Complete Phase 2 availability management system:
    - Admin availability dashboard at /availability with room selector, calendar, and settings panel
    - Guest-facing read-only calendar stub at /rooms/[id]
    - "Availability" nav item in the admin sidebar
    - Server actions: toggleBlockedDate, saveBlockedRange, updateRoomAvailabilitySettings
    - Prisma schema: BlockedDate model + Room availability fields (bookingWindowMonths, minStayNights, maxStayNights)
  </what-built>
  <how-to-verify>
    With the dev server running at http://localhost:3000, verify each scenario:

    **Scenario 1: Admin sidebar navigation**
    - Log in to the admin dashboard
    - Confirm "Availability" appears in the left sidebar between Rooms and Settings
    - Click it — should navigate to /availability

    **Scenario 2: Room selector**
    - On /availability, confirm a room selector dropdown appears at the top
    - If multiple rooms exist: select a different room — calendar should reload showing that room's data

    **Scenario 3: Single-date blocking (AVAIL-02)**
    - Click any available date on the calendar
    - Confirm the date immediately changes visual style (no page reload needed)
    - Refresh the page — confirm the date is still shown as blocked
    - Click the blocked date again — confirm it unblocks

    **Scenario 4: Range blocking (AVAIL-02)**
    - Click a start date — it should become highlighted
    - Click an end date (2-5 days later) — "Block Range" and "Unblock Range" buttons should appear
    - Click "Block Range" — all dates in range should become blocked
    - Refresh — confirm all dates in range are still blocked
    - Click "Unblock Range" — all dates in range should unblock

    **Scenario 5: Settings panel (AVAIL-03, AVAIL-04)**
    - In the settings panel, change Min stay nights to 3
    - Change Booking window to 6 months
    - Click Save — confirm "Saved" feedback appears
    - Refresh the page — confirm the updated values are shown

    **Scenario 6: Guest calendar (AVAIL-01)**
    - Open a new incognito tab (or log out)
    - Navigate to http://localhost:3000/rooms/[some-valid-room-id]
    - Confirm the page loads WITHOUT redirecting to /login
    - Confirm the room name appears as the heading
    - Confirm past dates are greyed out
    - Confirm "Minimum stay: X nights" text appears below the calendar
    - Confirm dates blocked in Scenario 3 are visually distinct (greyed/strikethrough) on the guest calendar

    **Scenario 7: Booking window enforcement (AVAIL-03)**
    - With booking window set to 3 months (from Scenario 5): dates more than 3 months from today should be greyed out on the guest calendar
  </how-to-verify>
  <resume-signal>
    Type "approved" when all scenarios pass.
    Or describe any issues found for investigation.
  </resume-signal>
</task>

</tasks>

<verification>
All Phase 2 requirements verified:
- AVAIL-01: Guest can view availability calendar at /rooms/[id]
- AVAIL-02: Landlord can block/unblock dates at /availability
- AVAIL-03: Landlord can set booking window per room
- AVAIL-04: Landlord can set min/max stay per room
- ADMIN-04: Full admin availability management available

npm test and npm run build both exit 0.
</verification>

<success_criteria>
- Human verification: all 7 scenarios pass
- npm test exits 0 (full suite green)
- npm run build exits 0
- All Phase 2 requirements (AVAIL-01, AVAIL-02, AVAIL-03, AVAIL-04, ADMIN-04) confirmed working
</success_criteria>

<output>
After completion, create `.planning/phases/02-availability-management/02-04-SUMMARY.md`
</output>
