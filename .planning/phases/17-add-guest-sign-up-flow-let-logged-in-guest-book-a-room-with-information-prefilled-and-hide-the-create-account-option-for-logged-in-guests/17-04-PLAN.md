# Plan 17-04: Verification Checkpoint

## Goal
Confirm Phase 17 is complete: TypeScript compiles clean, build passes, and all user-facing flows work end-to-end.

## Tasks

### Task 1 — TypeScript and build
```bash
npx tsc --noEmit
npm run build
```
Both must pass with zero errors.

### Task 2 — Human verification checklist

Work through each scenario in the browser:

**Sign-up flow**
- [ ] Navigate to `/guest/signup` — page renders with dark background, Bebas Neue headline "Create Account", four fields (Name, Email, Phone, Password)
- [ ] Submit with a new email → redirected to `/my-bookings`, signed in
- [ ] Submit with an existing email → error "An account with this email already exists."
- [ ] Visit `/guest/login` — "No account? Create one" link appears below forgot-password; clicking navigates to `/guest/signup`

**Home page**
- [ ] Home page footer strip shows both "Create account" text link and "My Bookings →" ghost button side-by-side
- [ ] "Create account" link navigates to `/guest/signup`

**Booking form — logged-out state**
- [ ] Visit `/rooms/[id]/book` while signed out
- [ ] Nav shows "Sign In" + "Sign Up" ghost buttons (not "My Bookings")
- [ ] Section 4 fields are empty and editable
- [ ] Section 5 shows "Save my booking to an account" checkbox

**Booking form — logged-in state**
- [ ] Sign in as a guest with a known name/email/phone, then visit `/rooms/[id]/book`
- [ ] Nav shows "My Bookings" → `/my-bookings` (not login redirect)
- [ ] Section 4 fields are prefilled from user_metadata and read-only
- [ ] Section 5 is replaced with "✓ Signed in as [name] · [email]" banner with sign-out link
- [ ] Submit a booking request → booking created, redirected to `/bookings/[id]`
- [ ] Navigate to `/my-bookings` → the new booking appears in the list

**Sign-out from booking form banner**
- [ ] Click sign-out link in the banner → signed out, page reloads showing logged-out state

---

## Success Criteria (Phase 17)
All checkboxes above pass. `tsc --noEmit` and `npm run build` both exit 0.
