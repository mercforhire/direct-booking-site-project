---
phase: 01.5-supabase-migration
plan: 05
type: execute
wave: 5
depends_on:
  - 01.5-04
files_modified: []
autonomous: false
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Landlord can visit /login, enter their email, and receive a magic link email"
    - "Clicking the magic link opens the app and lands on /dashboard (authenticated)"
    - "Visiting /dashboard without a session redirects to /login"
    - "All protected routes (/dashboard, /rooms, /settings) redirect unauthenticated users to /login"
    - "All existing admin features (room list, create/edit room, photo upload, settings) work after migration"
    - "UploadThing photo upload still works end-to-end"
  artifacts:
    - path: "src/lib/supabase/client.ts"
      provides: "Browser Supabase client"
    - path: "src/lib/supabase/server.ts"
      provides: "Server Supabase client"
    - path: "src/middleware.ts"
      provides: "Route protection"
    - path: "src/app/auth/confirm/route.ts"
      provides: "Magic link callback"
  key_links:
    - from: "Magic link email"
      to: "/auth/confirm?token_hash=...&type=email"
      via: "Supabase email delivery via Resend SMTP"
      pattern: "verifyOtp"
    - from: "/auth/confirm"
      to: "/dashboard"
      via: "verifyOtp success → NextResponse.redirect"
      pattern: "redirect.*dashboard"
---

<objective>
Human end-to-end verification of the complete Supabase Auth migration — magic link login flow, route protection, and all admin features.

Purpose: The migration touches middleware, auth, and all server actions. A full end-to-end human test is the only way to confirm the system works as a whole, since middleware redirects and magic link email delivery cannot be unit-tested in isolation.
Output: Verified working system — landlord can log in and use all admin features.
</objective>

<execution_context>
@/Users/LeonChen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/LeonChen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run automated checks</name>
  <files>package.json, prisma/schema.prisma, src/middleware.ts, src/lib/supabase/client.ts, src/lib/supabase/server.ts</files>
  <action>
    Run the following checks before the human verification step:

    1. TypeScript clean build:
    ```bash
    npx tsc --noEmit --skipLibCheck
    ```
    Must exit with 0 errors.

    2. No next-auth remnants:
    ```bash
    grep -rn "next-auth\|@auth/prisma-adapter\|from.*@/lib/auth\"" src/ --include="*.ts" --include="*.tsx"
    ```
    Must return no output.

    3. Prisma schema validates:
    ```bash
    npx prisma validate
    ```
    Must pass.

    4. Start the dev server:
    ```bash
    npm run dev
    ```
    Server must start without errors (watch for startup errors in the terminal; the server should be accessible at http://localhost:3000).

    If any check fails, fix the issue before proceeding to the human verification step.
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck && npx prisma validate && echo "PASS: build and schema clean"</automated>
  </verify>
  <done>TypeScript build is clean. No next-auth imports in src/. Prisma schema validates. Dev server starts without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of Supabase Auth and admin features</name>
  <what-built>
    Complete Supabase Auth migration: magic link login, route protection middleware, auth/confirm callback, server actions with Supabase session guards, and all existing admin features preserved.
  </what-built>
  <how-to-verify>
    With the dev server running at http://localhost:3000, test in this order:

    **1. Route protection (unauthenticated):**
    - Visit http://localhost:3000/dashboard in an incognito window
    - Expected: Immediately redirected to http://localhost:3000/login
    - Visit http://localhost:3000/rooms — Expected: redirected to /login
    - Visit http://localhost:3000/settings — Expected: redirected to /login

    **2. Magic link login flow:**
    - Go to http://localhost:3000/login
    - Enter the landlord email address (the one you created in Supabase Auth)
    - Click "Send Magic Link"
    - Expected: Page shows "Check your email" confirmation
    - Check that email inbox — you should receive an email from your configured sender address (via Resend SMTP)
    - Click the magic link in the email
    - Expected: Browser redirects to http://localhost:3000/dashboard and you are logged in

    **3. Protected routes accessible when logged in:**
    - After login, navigate to /rooms — Expected: Room list loads normally
    - Navigate to /settings — Expected: Settings page loads normally

    **4. Admin features work (room CRUD):**
    - Create a new room (fill all fields, submit)
    - Expected: Room appears in the room list
    - Edit the room (change name or rate)
    - Expected: Changes saved successfully

    **5. Photo upload still works:**
    - Open an existing room's edit page
    - Upload a photo using the photo uploader
    - Expected: Photo appears in the list, no UploadThing errors in console

    **6. Settings save:**
    - Visit /settings, change service fee or deposit amount, save
    - Expected: Values persist after page refresh

    If anything fails, note the exact step and error message.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 steps pass. Or describe exactly which step failed and what error you saw.</resume-signal>
</task>

</tasks>

<verification>
All six verification scenarios pass:
1. Unauthenticated access to /dashboard, /rooms, /settings redirects to /login
2. Magic link email is received and clicking it establishes a session and lands on /dashboard
3. Authenticated user can access /dashboard, /rooms, /settings
4. Room create and edit work
5. Photo upload works
6. Settings save and persist
</verification>

<success_criteria>
Landlord can log in via magic link email and access all admin features. All protected routes redirect unauthenticated users. UploadThing photo upload works end-to-end. The Supabase Auth migration is complete and INFRA-01 is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-supabase-migration/01.5-05-SUMMARY.md`
</output>
