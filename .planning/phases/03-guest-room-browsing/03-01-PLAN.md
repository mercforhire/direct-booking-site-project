---
phase: 03-guest-room-browsing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/lib/availability-filter.test.ts
  - tests/lib/rooms.test.ts
  - src/lib/availability-filter.ts
  - src/lib/room-formatters.ts
  - src/middleware.ts
  - src/app/rooms/page.tsx
  - src/components/guest/room-list-filter.tsx
  - src/components/guest/room-list.tsx
  - src/components/guest/room-tile.tsx
autonomous: true
requirements:
  - ROOM-01
  - ROOM-02
  - ROOM-04

must_haves:
  truths:
    - "Guest can navigate to /rooms without being redirected to /login"
    - "Guest can see all active rooms listed as full-width tiles with cover photo, name, rate, and max capacity"
    - "Guest can enter dates and guest count in the filter header and see unavailable rooms grayed out with 'Unavailable for these dates' badge"
    - "Filter state is reflected in URL query params (?checkin=...&checkout=...&guests=N)"
    - "Greyed-out rooms are still clickable and navigate to the detail page"
    - "When all rooms are unavailable, an empty state message prompts the user to try different dates"
  artifacts:
    - path: "src/lib/availability-filter.ts"
      provides: "isRoomAvailable pure function used by the room list client component"
      exports: ["isRoomAvailable"]
    - path: "src/lib/room-formatters.ts"
      provides: "Decimal-to-Number coercion helpers for RSC boundary"
      exports: ["coerceRoomDecimals", "coerceAddOnDecimals"]
    - path: "src/app/rooms/page.tsx"
      provides: "/rooms list page — RSC fetches all active rooms + blocked dates, passes to RoomList"
    - path: "src/components/guest/room-list-filter.tsx"
      provides: "Date range picker + guest count input, updates URL params on change"
    - path: "src/components/guest/room-list.tsx"
      provides: "Client component — reads URL params, filters rooms, renders tiles"
    - path: "src/components/guest/room-tile.tsx"
      provides: "Single room tile — cover photo, name, rate, capacity, unavailable badge"
    - path: "tests/lib/availability-filter.test.ts"
      provides: "Unit tests for isRoomAvailable pure function"
    - path: "tests/lib/rooms.test.ts"
      provides: "Unit tests for Decimal coercion and fee row formatting helpers"
  key_links:
    - from: "src/app/rooms/page.tsx"
      to: "src/components/guest/room-list.tsx"
      via: "RSC passes coerced room array as prop"
      pattern: "<RoomList rooms={roomsForClient}"
    - from: "src/components/guest/room-list.tsx"
      to: "src/lib/availability-filter.ts"
      via: "isRoomAvailable called per room tile render"
      pattern: "isRoomAvailable\\(room"
    - from: "src/middleware.ts"
      to: "isRoomsAdminRoute check"
      via: "pathname === '/rooms' removed — /rooms is now a public guest route"
      pattern: "pathname.*\\/rooms.*\\/new.*\\/edit"
---

<objective>
Build the /rooms list page and the client-side availability filtering system.

Purpose: Guests can browse all available rooms, filter by dates and guest count, and see which rooms are available — all from a public (no-auth) route.
Output: Working /rooms list page with URL-param-driven filter, greyed-out unavailable tiles, and test coverage for the availability filter and decimal coercion logic.
</objective>

<execution_context>
@/Users/LeonChen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/LeonChen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-guest-room-browsing/03-CONTEXT.md
@.planning/phases/03-guest-room-browsing/03-RESEARCH.md

@src/middleware.ts
@src/app/rooms/[id]/page.tsx
@tests/lib/prisma-mock.ts
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/lib/prisma.ts (established pattern):
```typescript
import { PrismaClient } from "@prisma/client"
export const prisma = new PrismaClient()
```

Room model fields (from prisma/schema.prisma):
```
id, name, description, location, baseNightlyRate (Decimal), cleaningFee (Decimal),
extraGuestFee (Decimal), baseGuests (Int), maxGuests (Int), isActive (Boolean),
bookingWindowMonths (Int), minStayNights (Int), maxStayNights (Int)
Relations: photos (RoomPhoto[]), addOns (AddOn[]), blockedDates (BlockedDate[])
```

RoomPhoto: { id, url, position (Int), roomId }
BlockedDate: { id, date (DateTime @db.Date), roomId }
AddOn: { id, name, price (Decimal), roomId }

Established patterns to follow:
- export const dynamic = "force-dynamic" on all RSC pages that query DB
- Number(r.baseNightlyRate) for Decimal-to-Number at RSC boundary
- b.date.toLocaleDateString("en-CA") for YYYY-MM-DD in local time (NOT toISOString)
- useSearchParams() must be wrapped in <Suspense> in the parent RSC
- DayPicker range mode: mode="range", selected: DateRange | undefined, onSelect: (r: DateRange | undefined) => void

Current middleware isRoomsAdminRoute (must be changed):
```typescript
// BEFORE (incorrect — blocks guest access to /rooms):
const isRoomsAdminRoute =
  pathname === "/rooms" ||           // <-- remove this line
  pathname.startsWith("/rooms/new") ||
  pathname.endsWith("/edit")

// AFTER (correct):
const isRoomsAdminRoute =
  pathname.startsWith("/rooms/new") ||
  pathname.endsWith("/edit")
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffolds — availability filter + room formatter helpers</name>
  <files>
    src/lib/availability-filter.ts
    src/lib/room-formatters.ts
    tests/lib/availability-filter.test.ts
    tests/lib/rooms.test.ts
  </files>
  <behavior>
    availability-filter.test.ts cases:
    - isRoomAvailable returns true when no checkin/checkout provided
    - isRoomAvailable returns false when maxGuests < guests param
    - isRoomAvailable returns false when a date in the range is in blockedDateStrings
    - isRoomAvailable returns false when checkout exceeds bookingWindowMonths from today
    - isRoomAvailable returns true for a clean range with no conflicts
    - isRoomAvailable checks up to but NOT including checkout date (checkout day is departure, not a blocked night)

    rooms.test.ts cases:
    - coerceRoomDecimals converts baseNightlyRate, cleaningFee, extraGuestFee from string Decimal representation to number
    - coerceAddOnDecimals converts price from string representation to number
    - coerced values are plain numbers (typeof === "number"), not Decimal instances
  </behavior>
  <action>
    RED phase: Write tests first. Both test files import functions that don't exist yet — tests must fail on first run.

    Then GREEN phase: Create the implementation files.

    src/lib/availability-filter.ts:
    - Export: isRoomAvailable(room: { blockedDateStrings: string[]; bookingWindowMonths: number; maxGuests: number }, checkin: string, checkout: string, guests: number): boolean
    - If checkin or checkout is empty string → return true (no filter applied)
    - Guest count check: if room.maxGuests < guests → return false
    - Booking window check: compute windowEnd = new Date(), windowEnd.setMonth(windowEnd.getMonth() + room.bookingWindowMonths), if new Date(checkout + "T00:00:00") > windowEnd → return false
    - Blocked date overlap: build Set from blockedDateStrings, iterate cursor from checkin (inclusive) to checkout (exclusive) using setDate(+1), get each date as cursor.toLocaleDateString("en-CA"), check against Set
    - Return true if all checks pass

    src/lib/room-formatters.ts:
    - Export: coerceRoomDecimals(r: { baseNightlyRate: unknown; cleaningFee: unknown; extraGuestFee: unknown; [key: string]: unknown }) — returns same object with those three fields as Number()
    - Export: coerceAddOnDecimals(a: { price: unknown; [key: string]: unknown }) — returns same object with price as Number()
    - These are thin wrappers over Number() to centralize the coercion pattern

    Run npm test after implementation — all tests must pass.
  </action>
  <verify>
    <automated>npm test -- tests/lib/availability-filter.test.ts tests/lib/rooms.test.ts</automated>
  </verify>
  <done>All tests green. isRoomAvailable handles all 6 cases. coerceRoomDecimals and coerceAddOnDecimals convert Decimal-like values to numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Middleware fix + /rooms list page + room list components</name>
  <files>
    src/middleware.ts
    src/app/rooms/page.tsx
    src/components/guest/room-list-filter.tsx
    src/components/guest/room-list.tsx
    src/components/guest/room-tile.tsx
  </files>
  <action>
    STEP A — Fix middleware (src/middleware.ts):
    Remove `pathname === "/rooms" ||` from the isRoomsAdminRoute check. Leave `/rooms/new` and `/edit` protections intact. The /rooms guest list page is public — no auth required.

    STEP B — /rooms page (src/app/rooms/page.tsx):
    - export const dynamic = "force-dynamic"
    - Fetch all active rooms in a single Prisma query with nested selects:
      id, name, location, baseNightlyRate, cleaningFee, extraGuestFee, baseGuests, maxGuests, bookingWindowMonths
      photos: { select: { url, position }, orderBy: { position: asc }, take: 1 }
      blockedDates: { select: { date } }
    - At RSC boundary, map rooms to coerce Decimals using coerceRoomDecimals from src/lib/room-formatters.ts
    - Convert blockedDates to blockedDateStrings: b.date.toLocaleDateString("en-CA") (NOT toISOString)
    - Pass the resulting roomsForClient array to <RoomList rooms={roomsForClient} /> wrapped in <Suspense fallback={<RoomListSkeleton />}>
    - RoomListSkeleton: 3 rows of skeleton divs (h-32, full-width, rounded), animate-pulse
    - Page title: simple <h1>Rooms</h1> or leave to RoomList to render its own header

    STEP C — RoomListFilter (src/components/guest/room-list-filter.tsx):
    - "use client"
    - Props: none (reads URL params internally)
    - useSearchParams, useRouter, usePathname from next/navigation
    - Local state: range (DateRange | undefined) from react-day-picker, guests (number, default 1)
    - DayPicker with mode="range", selected=range, onSelect=handleRangeSelect
      - disabled: [{ before: new Date() }] (can't pick past dates)
      - When r?.from && r?.to both set → call updateFilter with checkin/checkout as toLocaleDateString("en-CA")
    - Guest count: <input type="number" min="1" value={guests} onChange={...} /> — on change call updateFilter({ guests: String(n) })
    - updateFilter(updates): builds URLSearchParams from current params, applies updates, router.push to ?params
    - Clear button: resets range + guests + removes all three params from URL
    - Layout: horizontal flex row on desktop, stacked on mobile. Always visible, no collapse. Prominent header section at top of the page.

    STEP D — RoomList (src/components/guest/room-list.tsx):
    - "use client"
    - Props: rooms (the coerced array from RSC)
    - useSearchParams to read checkin, checkout, guests
    - For each room, call isRoomAvailable(room, checkin, checkout, Number(guests)) from src/lib/availability-filter.ts
    - Sort: available rooms first, unavailable rooms second (maintain original order within each group)
    - If ALL rooms are unavailable AND checkin/checkout are set: show empty state — "No rooms available for those dates. Try different dates or fewer guests." centered, with a clear link/button
    - Otherwise: render <RoomListFilter /> at top, then map rooms to <RoomTile room={room} isAvailable={bool} searchParams={searchParams.toString()} />

    STEP E — RoomTile (src/components/guest/room-tile.tsx):
    - Can be "use client" (receives isAvailable as prop)
    - Props: room (coerced room object with photo?: { url, position }), isAvailable (boolean), searchParams (string)
    - Link href: /rooms/${room.id}${searchParams ? "?" + searchParams : ""}
    - Layout: full-width horizontal tile — photo on left (fixed w-48 h-32 or similar), info on right
    - Photo: next/image with fill, object-cover. If room.photos[0] is undefined → grey placeholder div with a simple house/room icon (lucide-react `Home` icon, centered)
    - Info section: room name (font-semibold), "$X / night" (baseNightlyRate.toFixed(2)), "Up to N guests" (maxGuests)
    - Unavailable state: when isAvailable=false → apply opacity-50 to the entire tile, show <Badge variant="secondary">Unavailable for these dates</Badge> overlaid or below the name. Tile is still a <Link> (clickable).
    - Wrap entire tile in shadcn Card with hover:shadow-md transition

    Run npm test after all changes — full suite must pass.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - Navigating to /rooms (unauthenticated) does NOT redirect to /login
    - All active rooms render as full-width horizontal tiles
    - Filter header is visible at top with date picker and guest count
    - Selecting dates that overlap blocked rooms greys those tiles with "Unavailable for these dates"
    - URL updates to ?checkin=...&checkout=...&guests=N on filter change
    - npm test passes
  </done>
</task>

</tasks>

<verification>
npm test passes (all existing tests + new tests green).
TypeScript build passes: npx tsc --noEmit
Middleware: /rooms accessible without auth (no redirect to /login).
</verification>

<success_criteria>
1. npm test green — availability-filter.test.ts + rooms.test.ts all passing
2. Guest can reach /rooms without authentication
3. Room tiles display cover photo (or placeholder), name, rate, capacity
4. Filter updates URL params and greys out unavailable rooms
5. Greyed tiles still navigate to detail page
</success_criteria>

<output>
After completion, create .planning/phases/03-guest-room-browsing/03-01-SUMMARY.md
</output>
