---
phase: 05-approval-flow-notifications
plan: 05
type: execute
wave: 4
depends_on:
  - 05-04
files_modified: []
autonomous: false
requirements:
  - APPR-01
  - APPR-02
  - APPR-03
  - APPR-04
  - APPR-05
  - ADMIN-01

must_haves:
  truths:
    - "Full test suite passes with zero failures"
    - "Landlord receives email on new booking request (APPR-01)"
    - "Landlord can approve a PENDING booking and set confirmedPrice (APPR-02)"
    - "Landlord can decline a PENDING booking with optional reason (APPR-03)"
    - "Guest receives approval email with confirmed price and booking URL (APPR-04)"
    - "Guest receives decline email with optional reason and booking URL (APPR-05)"
    - "Admin bookings dashboard shows all bookings by status tab (ADMIN-01)"
  artifacts:
    - path: "tests/actions/booking-admin.test.ts"
      provides: "Full green unit test suite for APPR-02 through APPR-05"
    - path: "tests/actions/booking.test.ts"
      provides: "Passing tests including APPR-01 landlord notification"
  key_links:
    - from: "npx vitest run"
      to: "all test files"
      via: "full suite run"
      pattern: "PASS"
---

<objective>
Phase gate: full automated test suite verification followed by a human end-to-end verification checkpoint covering all 6 Phase 5 requirements.

Purpose: Confirm the entire phase works end-to-end — automated tests for server action logic, human verification for the email and UI flows that cannot be unit-tested.
Output: Green test suite + human sign-off on all Phase 5 requirements.
</objective>

<execution_context>
@/Users/LeonChen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/LeonChen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-approval-flow-notifications/05-VALIDATION.md
@.planning/phases/05-approval-flow-notifications/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite run</name>
  <files></files>
  <action>
Run the full Vitest test suite. All tests must pass with zero failures and zero errors.

If any tests are failing:
1. Read the failure output carefully
2. Identify whether the failure is in booking.test.ts (APPR-01 retrofit), booking-admin.test.ts (APPR-02 through APPR-05), or pre-existing tests
3. Fix the root cause — do not skip or comment out failing tests
4. Re-run until the full suite is green

Common failure modes to check:
- booking.test.ts: mockCreatedBooking missing `room` property after Plan 03 added room include to prisma.booking.create
- booking-admin.test.ts: mock for @react-email/render missing if Plan 02 did not add it
- booking-admin.test.ts: Supabase server mock not matching the createClient factory signature
- TypeScript errors surfacing as test failures (check npx tsc --noEmit first)
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>npx vitest run completes with 0 failures and 0 errors. All test files pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5 implementation:
- APPR-01: Landlord email notification on new booking request (submitBooking retrofit)
- APPR-02: approveBooking server action + admin UI approve form
- APPR-03: declineBooking server action + admin UI decline form
- APPR-04: BookingApprovedEmail template sent to guest on approval
- APPR-05: BookingDeclinedEmail template sent to guest on decline
- ADMIN-01: /bookings admin dashboard with status tabs + /bookings/[id] detail page
  </what-built>
  <how-to-verify>
**Setup:** Ensure dev server is running (`npm run dev`) and LANDLORD_EMAIL is set in .env.local.

**Scenario 1 — APPR-01: Landlord notification on new booking:**
1. Submit a new booking request as a guest at /rooms/[id]/book
2. Check the LANDLORD_EMAIL inbox — verify an email arrived with subject "New booking request from [Guest Name]"
3. Verify email contains: guest name, room name, check-in/check-out dates, number of guests, estimated total, link to /bookings/[id]

**Scenario 2 — ADMIN-01: Admin bookings dashboard:**
4. Log in as admin and navigate to /bookings via the sidebar
5. Verify "Bookings" appears in the sidebar nav with the clipboard icon
6. Verify six status tabs: All, Pending, Approved / Awaiting Payment, Declined, Paid, Completed, Cancelled
7. Verify the booking from Scenario 1 appears in the Pending tab with correct details
8. Verify clicking the booking row opens /bookings/[id]

**Scenario 3 — APPR-02 + APPR-04: Approve a booking:**
9. On the /bookings/[id] detail page for the PENDING booking, enter a confirmed price (e.g. 850)
10. Click Approve — verify AlertDialog confirmation appears
11. Confirm — verify booking status updates to "Approved / Awaiting Payment" in the UI
12. Verify Approve and Decline sections no longer appear on the detail page
13. Check guest email inbox — verify approval email arrived with subject containing "approved"
14. Verify email contains the confirmed price ($850.00 CAD) and a working link to /bookings/[id]?token=...

**Scenario 4 — APPR-03 + APPR-05: Decline a booking:**
15. Submit another new booking request as a guest
16. On /bookings/[id] for that PENDING booking, leave decline reason as "Dates no longer available"
17. Click Decline — verify AlertDialog confirmation, confirm
18. Verify booking status updates to "Declined" in the UI
19. Check guest email inbox — verify decline email with reason "Dates no longer available"
20. Verify declined booking appears in the Declined tab on /bookings

**Scenario 5 — Stale state guard:**
21. Attempt to approve the booking from Scenario 3 (already APPROVED) by navigating directly to /bookings/[id]
22. Verify Approve/Decline forms do not appear for non-PENDING bookings

**Scenario 6 — Unauthenticated access:**
23. Log out and navigate to /bookings directly
24. Verify redirect to /login (middleware protection working)
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 scenarios pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 6 Phase 5 requirements verified:
- APPR-01: Landlord email confirmed in Scenario 1
- APPR-02: Approve action confirmed in Scenario 3
- APPR-03: Decline action confirmed in Scenario 4
- APPR-04: Guest approval email confirmed in Scenario 3
- APPR-05: Guest decline email confirmed in Scenario 4
- ADMIN-01: Admin bookings dashboard confirmed in Scenario 2
</verification>

<success_criteria>
- `npx vitest run` — 0 failures, 0 errors
- All 6 human verification scenarios pass
- Phase 5 requirements APPR-01 through APPR-05 and ADMIN-01 marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/05-approval-flow-notifications/05-05-SUMMARY.md`
2. Update REQUIREMENTS.md: mark APPR-01, APPR-02, APPR-03, APPR-04, APPR-05, ADMIN-01 as complete ([x])
3. Update ROADMAP.md Phase 5 progress table: mark all plans complete, set status to Complete with today's date
</output>
