---
phase: 01.5-supabase-migration
verified: 2026-03-26T00:00:00Z
status: human_needed
score: 13/13 automated must-haves verified
re_verification: false
human_verification:
  - test: "Magic link login flow end-to-end"
    expected: "Landlord submits email on /login, receives email via Resend/Supabase SMTP, clicks link, lands on /dashboard authenticated"
    why_human: "Requires live Supabase project, live email delivery via Resend SMTP, and a real browser session — cannot be verified programmatically"
  - test: "Unauthenticated redirect for all three protected routes"
    expected: "Visiting /dashboard, /rooms, /settings without a session each redirect to /login"
    why_human: "Middleware redirect requires the full Next.js runtime — cannot be exercised in a static grep or TS check"
  - test: "Admin features work post-migration (room CRUD, settings save)"
    expected: "Create room, edit room, save settings all complete without error and persist to Supabase PostgreSQL"
    why_human: "Requires a live Supabase database session and authenticated browser session"
  - test: "UploadThing photo upload works end-to-end"
    expected: "Upload a photo from the room edit page — photo appears in gallery, renders via next/image without error"
    why_human: "Requires live auth session + UploadThing CDN + browser upload interaction. Note: SUMMARY-05 reports human-approved all 6 E2E scenarios."
---

# Phase 1.5: Supabase Migration — Verification Report

**Phase Goal:** App runs on Supabase managed PostgreSQL with Supabase Auth replacing NextAuth — landlord can log in via magic link, all existing admin features work unchanged
**Verified:** 2026-03-26
**Status:** human_needed (all automated checks pass; live E2E items need human confirmation)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Supabase project exists with credentials available in .env.local | ? NEEDS HUMAN | .env.local is gitignored; SUMMARY-01 reports human configured all 5 vars. Cannot inspect at rest. |
| 2 | @supabase/supabase-js and @supabase/ssr packages are installed | VERIFIED | package.json line 23-24: `@supabase/ssr: ^0.9.0`, `@supabase/supabase-js: ^2.100.1` |
| 3 | next-auth and @auth/prisma-adapter packages are removed | VERIFIED | Neither package appears in package.json dependencies or devDependencies |
| 4 | Prisma schema has no NextAuth models (Account, Session, User, VerificationToken) | VERIFIED | prisma/schema.prisma contains only Room, RoomPhoto, AddOn, Settings — zero NextAuth models |
| 5 | Prisma datasource uses directUrl (DIRECT_URL env var) | VERIFIED | prisma/schema.prisma line 8: `directUrl = env("DIRECT_URL")` |
| 6 | Browser Supabase client (createClient) available for client components | VERIFIED | src/lib/supabase/client.ts exports `createClient` using `createBrowserClient` from @supabase/ssr |
| 7 | Server Supabase client (async createClient) available for server-side code | VERIFIED | src/lib/supabase/server.ts exports `async function createClient()` awaiting `cookies()` then `createServerClient` |
| 8 | Middleware protects /dashboard, /rooms, /settings using getUser() | VERIFIED | src/middleware.ts: `adminPaths = ["/dashboard", "/rooms", "/settings"]`, guards with `supabase.auth.getUser()`, not getSession() |
| 9 | Unauthenticated requests to admin routes redirect to /login | VERIFIED (code) | Middleware: `if (!user && isAdminRoute) ... url.pathname = "/login"; return NextResponse.redirect(url)`. Live behavior needs human. |
| 10 | /auth/confirm route handler exchanges PKCE code for session | VERIFIED | src/app/auth/confirm/route.ts calls `supabase.auth.exchangeCodeForSession(code)` (PKCE fix applied in Plan 05) |
| 11 | All three server actions use Supabase getUser() for auth | VERIFIED | room.ts, settings.ts, room-photos.ts all import `createClient` from `@/lib/supabase/server` and call `supabase.auth.getUser()` |
| 12 | Login page calls signInWithOtp with shouldCreateUser: false | VERIFIED | src/app/(auth)/login/page.tsx: `supabase.auth.signInWithOtp({ email, options: { shouldCreateUser: false, emailRedirectTo: .../auth/confirm } })` |
| 13 | Old NextAuth files are deleted | VERIFIED | src/lib/auth.ts, src/lib/auth-edge.ts, src/app/api/auth/[...nextauth]/route.ts — all confirmed absent |
| 14 | No remaining next-auth imports in src/ | VERIFIED | grep for `next-auth`, `@auth/prisma-adapter` across entire src/ returns zero matches |
| 15 | UploadThing middleware uses Supabase auth | VERIFIED | src/app/api/uploadthing/core.ts imports `createClient` from `@/lib/supabase/server`, calls `getUser()` |
| 16 | Sidebar uses Supabase signOut | VERIFIED | src/components/admin/sidebar.tsx: `supabase.auth.signOut()` then `router.push("/login")` — no next-auth signOut |
| 17 | UploadThing CDN hostnames configured for next/image | VERIFIED | next.config.ts: `remotePatterns` includes `*.ufs.sh` wildcard hostname |

**Automated score:** 16/17 truths verified programmatically (Truth 1 requires human — .env.local is gitignored by design)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `package.json` | @supabase packages, no next-auth | VERIFIED | `@supabase/ssr ^0.9.0`, `@supabase/supabase-js ^2.100.1` present; `next-auth` and `@auth/prisma-adapter` absent |
| `prisma/schema.prisma` | directUrl, no NextAuth models, all 4 app models | VERIFIED | `directUrl = env("DIRECT_URL")` on line 8; Room, RoomPhoto, AddOn, Settings present; no Account/Session/User/VerificationToken |
| `src/lib/supabase/client.ts` | createClient for browser | VERIFIED | 7 lines, exports `createClient` using `createBrowserClient`; not a stub |
| `src/lib/supabase/server.ts` | async createClient for server | VERIFIED | 27 lines, exports `async function createClient()`; awaits `cookies()`; full cookie get/set implementation |
| `src/middleware.ts` | Supabase SSR middleware with getUser() | VERIFIED | 53 lines; inline `createServerClient`; `getUser()`; protects all 3 admin paths; correct matcher config |
| `src/app/auth/confirm/route.ts` | PKCE magic link callback | VERIFIED | 19 lines; `exchangeCodeForSession(code)` (PKCE-correct); redirects to /dashboard or /login?error=invalid_token |
| `src/actions/room.ts` | Room CRUD with Supabase auth guard | VERIFIED | `requireAuth()` uses `createClient` + `getUser()`; all CRUD operations use Prisma unchanged |
| `src/actions/settings.ts` | Settings action with Supabase auth guard | VERIFIED | Inline `createClient()` + `getUser()` guard before Prisma upsert |
| `src/actions/room-photos.ts` | Photo actions with Supabase auth guard | VERIFIED | `requireAuth()` uses `createClient` + `getUser()`; add/reorder/delete with Prisma + UTApi |
| `src/app/(auth)/login/page.tsx` | Magic link OTP login page | VERIFIED | `signInWithOtp` with `shouldCreateUser: false`; full UI with sent-state confirmation |
| `next.config.ts` | UploadThing CDN in remotePatterns | VERIFIED | `*.ufs.sh` in remotePatterns |
| `src/app/api/uploadthing/core.ts` | UploadThing middleware with Supabase auth | VERIFIED | `createClient` + `getUser()` in middleware function |
| `src/components/admin/sidebar.tsx` | Supabase signOut in sidebar | VERIFIED | `supabase.auth.signOut()` + `router.push("/login")`; no next-auth reference |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/middleware.ts` | Supabase Auth server | `createServerClient` inline + `getUser()` | VERIFIED | createServerClient called directly (not shared factory); `supabase.auth.getUser()` on line 35 |
| `src/app/auth/confirm/route.ts` | `src/lib/supabase/server` | `import createClient, call exchangeCodeForSession` | VERIFIED | `import { createClient } from "@/lib/supabase/server"`; `supabase.auth.exchangeCodeForSession(code)` on line 11 |
| `src/app/(auth)/login/page.tsx` | `src/lib/supabase/client.ts` | `createClient() → signInWithOtp` | VERIFIED | `import { createClient } from "@/lib/supabase/client"`; `supabase.auth.signInWithOtp(...)` on line 23 |
| `src/actions/room.ts` | `src/lib/supabase/server` | `import createClient, call getUser()` | VERIFIED | `import { createClient } from "@/lib/supabase/server"` on line 3; `supabase.auth.getUser()` on line 10 |
| `src/actions/settings.ts` | `src/lib/supabase/server` | `import createClient, call getUser()` | VERIFIED | Same import pattern; `getUser()` on line 10 |
| `src/actions/room-photos.ts` | `src/lib/supabase/server` | `import createClient, call getUser()` | VERIFIED | Same import pattern; `getUser()` on line 12 |
| `src/app/api/uploadthing/core.ts` | `src/lib/supabase/server` | `import createClient, call getUser()` | VERIFIED | `import { createClient } from "@/lib/supabase/server"` on line 2; `getUser()` on line 10 |
| `src/components/admin/sidebar.tsx` | `src/lib/supabase/client.ts` | `createClient() → signOut()` | VERIFIED | `import { createClient } from "@/lib/supabase/client"` on line 5; `supabase.auth.signOut()` on line 23 |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| INFRA-01 | All 5 plans (01–05) | Auth infrastructure supports landlord-only access control (admin dashboard gated behind authentication) | VERIFIED (code) / NEEDS HUMAN (live flow) | Middleware guards all admin routes with `getUser()`; server actions guard all mutations; login page uses `signInWithOtp`; PKCE callback exchanges code for session. Live magic link flow confirmed by human in Plan 05 E2E checkpoint. |

**Note on INFRA-01 in REQUIREMENTS.md:** INFRA-01 does not appear in `.planning/REQUIREMENTS.md` (which tracks v1 product requirements ROOM-01 through MSG-05). INFRA-01 is defined in `.planning/ROADMAP.md` as the infrastructure requirement for Phase 1.5. This is not a gap — it is correctly scoped as an infrastructure prerequisite that enables all future phases, not a standalone product requirement for guests or admin users.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/app/(auth)/login/page.tsx` | 68 | `placeholder="landlord@example.com"` | None | HTML input placeholder attribute — not a code stub |

No code stubs, empty implementations, TODO/FIXME markers, or incomplete handlers found in any migration file.

---

### Human Verification Required

The automated analysis is fully positive. The following items cannot be verified without a live environment:

#### 1. Supabase Credentials Configured

**Test:** Inspect `.env.local` for the five required variables: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`, `DIRECT_URL`
**Expected:** All five present; `DATABASE_URL` and `DIRECT_URL` both point to `db.[project-ref].supabase.co:5432` (direct connection, per SUMMARY-02 deviation note)
**Why human:** `.env.local` is gitignored and cannot be read by the verifier

#### 2. Magic Link Login Flow

**Test:** Start the dev server, open an incognito window, go to `/login`, enter the landlord email, click "Send Magic Link", check email inbox for the link, click it
**Expected:** Browser lands on `/dashboard` with an active session — confirming PKCE `code` exchange works via Resend SMTP delivery
**Why human:** Requires live Supabase project + live email delivery + real browser

#### 3. Unauthenticated Route Protection (Live)

**Test:** In an incognito window (no session), visit `/dashboard`, `/rooms`, and `/settings` in turn
**Expected:** Each URL immediately redirects to `/login`
**Why human:** Middleware redirect requires the Next.js runtime — code is correct but live behavior must be confirmed

#### 4. Admin Features After Migration

**Test:** Log in via magic link, then: (a) create a room, (b) edit an existing room, (c) save settings
**Expected:** All operations complete without error and changes persist after page refresh
**Why human:** Requires authenticated Supabase session + live Supabase PostgreSQL writes

#### 5. UploadThing Photo Upload

**Test:** Log in, open a room's edit page, upload a new photo
**Expected:** Photo appears in the gallery and renders without next/image error (CDN URL via `*.ufs.sh`)
**Why human:** Requires live auth session + UploadThing CDN

**Note:** SUMMARY-05 reports that a human approved all 6 E2E scenarios (route protection, magic link login, protected routes, room CRUD, photo upload, settings save) during the Plan 05 verification checkpoint. If that human approval is trusted, all five human items above are already satisfied.

---

### Summary

The automated codebase analysis finds every migration artifact in place, substantive, and correctly wired:

- **Package swap:** Supabase packages installed, next-auth entirely removed from package.json and all source imports
- **Schema:** Prisma schema clean — only application models, directUrl configured for Supabase PgBouncer
- **Client utilities:** Browser and server Supabase clients match the canonical SSR pattern exactly
- **Middleware:** Protects all three admin paths using `getUser()` (JWT-validated, not cookie-spoofable)
- **Auth callback:** PKCE `exchangeCodeForSession` correctly implemented (PKCE bug fixed in Plan 05)
- **Server actions:** All three actions + UploadThing middleware use `createClient` + `getUser()` auth guard
- **Login page:** Uses `signInWithOtp` with `shouldCreateUser: false` (admin-only enforcement)
- **Sidebar:** Uses Supabase `signOut` — no next-auth reference anywhere
- **Deleted files:** src/lib/auth.ts, src/lib/auth-edge.ts, and the NextAuth route are confirmed absent
- **No next-auth remnants:** Zero matches across all src/ files

The only items requiring human confirmation are live-system behaviors (email delivery, middleware redirects at runtime, database writes, file uploads) — all of which SUMMARY-05 reports as human-verified during the Plan 05 E2E checkpoint.

---

_Verified: 2026-03-26_
_Verifier: Claude (gsd-verifier)_
