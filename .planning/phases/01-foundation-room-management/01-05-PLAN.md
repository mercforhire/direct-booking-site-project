---
phase: 01-foundation-room-management
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified: []
autonomous: false

requirements:
  - ADMIN-02
  - ADMIN-03
  - ADMIN-05

must_haves:
  truths:
    - "Visiting /dashboard while not signed in redirects to /login"
    - "After clicking a magic link, /dashboard shows the admin sidebar shell"
    - "Landlord can create a room and it appears in the /rooms table"
    - "Landlord can edit a room and upload photos with drag-to-reorder"
    - "Landlord can configure per-room fees and add-ons on the room edit form"
    - "Landlord can configure global settings and changes persist on reload"
  artifacts:
    - path: "src/app/(admin)/rooms/page.tsx"
      provides: "Room list table"
    - path: "src/components/forms/room-form.tsx"
      provides: "Room create/edit form with fees, add-ons, and photos"
    - path: "src/components/admin/photo-uploader.tsx"
      provides: "Sortable photo upload with deletion"
    - path: "src/app/(admin)/settings/page.tsx"
      provides: "Global settings form"
  key_links:
    - from: "Browser /login"
      to: "/dashboard"
      via: "Magic link email click"
      pattern: "auth redirect"
    - from: "Room form submit"
      to: "/rooms table"
      via: "createRoom server action + revalidatePath"
      pattern: "revalidatePath"
---

<objective>
Human visual verification of all Phase 1 work. Verifies the complete landlord admin experience end-to-end against a live development server with a real database and real UploadThing account.

Purpose: Automated tests cover server action logic. This checkpoint verifies the integrated UI experience: magic link flow, room CRUD, photo upload/reorder/delete, and global settings persistence.

Output: Human confirmation that Phase 1 is complete and ready for Phase 2.
</objective>

<execution_context>
@/Users/LeonChen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/LeonChen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-room-management/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start dev server</name>
  <files></files>
  <action>
    1. Run the full Vitest suite to confirm all tests are green before presenting the checkpoint:
       `npx vitest run`

    2. Start the development server:
       `npm run dev`

    3. Print setup reminders for the human verifier:
       - DATABASE_URL must point to a running PostgreSQL database
       - Run `npx prisma db push` to apply the schema if not done yet
       - AUTH_RESEND_KEY must be a valid Resend API key to test magic link delivery
       - UPLOADTHING_SECRET and UPLOADTHING_APP_ID must be configured for photo uploads

    If any test fails, stop and fix before presenting the checkpoint to the user.
  </action>
  <verify>
    <automated>npx vitest run 2>&1; echo "Vitest exit: $?"</automated>
  </verify>
  <done>
    - Full Vitest suite passes (exit 0)
    - Dev server is running on http://localhost:3000
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 admin experience:
    - Magic link authentication (/login → email → /dashboard)
    - Admin sidebar navigation (Dashboard, Rooms, Settings)
    - Room list data table (/rooms) with New Room button
    - Room create form at /rooms/new (name, description, location, rate, max guests, cleaning fee, extra guest fee, add-ons)
    - Room edit form at /rooms/[id] with all fields + photo upload/reorder/delete
    - Global settings form at /settings (service fee %, deposit amount)
  </what-built>
  <how-to-verify>
    Prerequisites: DATABASE_URL configured, `npx prisma db push` run, Resend/UploadThing keys set.

    1. Auth flow:
       a. Open http://localhost:3000/dashboard
       b. Confirm redirect to http://localhost:3000/login
       c. Enter your email and click "Send Magic Link"
       d. Check your inbox and click the link
       e. Confirm you land on /dashboard with the sidebar visible (Dashboard, Rooms, Settings, Sign Out)

    2. Room creation:
       a. Click "Rooms" in the sidebar → confirm /rooms loads with an empty table
       b. Click "New Room" → confirm the create form loads at /rooms/new
       c. Fill in: Name, Description, Location, Base Nightly Rate, Max Guests
       d. Set Cleaning Fee and Per Extra Guest Nightly Fee
       e. Click "Add Add-on" → add 2 add-ons (e.g. "Parking - $20", "Sofa Bed - $0")
       f. Click Save → confirm redirect to /rooms with the new room in the table
       g. Confirm the table shows: Name, Location, base rate ($X/night), Active badge, Edit link

    3. Room editing and photos:
       a. Click Edit on the room → confirm the edit form loads with all fields pre-filled
       b. Confirm the add-ons you entered appear in the dynamic list
       c. Confirm a "Save the room first, then add photos" message appears (or photo upload widget if photos exist)
       d. Upload 2–3 photos using the upload widget
       e. Confirm thumbnails appear with the first labelled "Cover"
       f. Drag a non-cover photo to the first position → confirm "Cover" label moves to the new first photo
       g. Reload the page → confirm the photo order is persisted correctly
       h. Click the X on a photo → confirm it disappears immediately

    4. Global settings:
       a. Click "Settings" in the sidebar
       b. Enter Service Fee: 3.5 and Deposit Amount: 150
       c. Click "Save Settings" → confirm "Settings saved." message appears
       d. Reload the page → confirm the form pre-fills with 3.5 and 150

    5. Sign out:
       a. Click "Sign Out" → confirm redirect to /login
       b. Confirm /dashboard redirects back to /login when accessed directly
  </how-to-verify>
  <resume-signal>
    Type "approved" if all verification steps pass.
    Or describe any issues found (e.g., "photos not reordering", "settings not persisting") and Claude will fix them.
  </resume-signal>
</task>

</tasks>

<verification>
All automated tests green (Task 1 confirms).
Human verifies the complete Phase 1 flow (Task 2).
</verification>

<success_criteria>
1. Full Vitest suite passes (npx vitest run exits 0)
2. All 5 human verification steps pass without issues:
   - Magic link login works end-to-end
   - Room CRUD with fees and add-ons works
   - Photo upload, reorder (persisted), and delete (CDN + DB) work
   - Global settings persist across page reload
   - Sign out redirects to login; unauthenticated access to /dashboard redirects to login
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-room-management/01-05-SUMMARY.md`

Then update `.planning/STATE.md`:
- status: Phase 1 complete
- stopped_at: Phase 1 complete — ready for Phase 2
- progress.completed_phases: 1
- progress.total_plans: 5
- progress.completed_plans: 5
- progress.percent: 11

Also update `.planning/ROADMAP.md`:
- Mark Phase 1 checkbox as complete: `- [x] **Phase 1: Foundation & Room Management**`
- Update the phase progress table row for Phase 1 to: "5/5 | Complete | [today's date]"
</output>
